interface:
  display_name: "ArcGIS Doc Lookup"
  short_description: "Find official ArcGIS docs and examples"
  default_prompt: "Use $arcgis-doc-lookup to detect the ArcGIS version in this app, then return matching official docs with concise implementation notes."
